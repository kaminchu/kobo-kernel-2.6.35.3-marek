image: ubuntu:14.04

stages:
  - build

build:
  stage: build
  script:
    - apt-get -y update && apt-get -y install build-essential gcc-arm-linux-gnueabi u-boot-tools
    - make CROSS_COMPILE=arm-linux-gnueabi- ARCH=arm uImage